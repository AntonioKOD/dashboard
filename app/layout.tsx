import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { GoogleAnalytics } from '@next/third-parties/google';
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "WW3 Dashboard - Global Conflict Monitoring",
  description: "Real-time monitoring of global conflicts, armed incidents, and geopolitical tensions worldwide",
  keywords: "conflict monitoring, global security, ACLED, UCDP, geopolitics, armed conflicts",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-slate-950 text-slate-100`}
      >
        {children}
        <GoogleAnalytics gaId="G-SJR8S5VZWW" />
      </body>
    </html>
  );
}
